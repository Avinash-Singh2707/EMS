@*@model IEnumerable<EMS.Models.Department>

    @{
        ViewBag.Title = "Index";
    }

    <h2>Index</h2>

    <p>
        @Html.ActionLink("Add New Department", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Dept_ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Dept_Name)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Dept_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Dept_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Dept_ID }, new { @class = "btn btn-primary btn-sm" }) |
            @Html.ActionLink("Details", "Details", new { id = item.Dept_ID }, new { @class = "btn btn-info btn-sm" }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Dept_ID }, new { @class = "btn btn-danger btn-sm" })
        </td>
    </tr>
    }

    </table>*@


@model IEnumerable<EMS.Models.Department>

@{
    ViewBag.Title = "Index";
}

<h2>Departments</h2>

<p>
    @Html.ActionLink("Add New Department", "Create", null, new { @class = "btn btn-primary" })
</p>

@if (TempData["AlertMessage"] != null)
{
    <div id="alertMessage" class="alert alert-success">
        <strong>Sucess !</strong>@TempData["AlertMessage"]
        <button type="button" class="close" data-dismiss="alert">
            <spam>&times;</spam>
        </button>
    </div>
}

<div class="table-responsive">
    <table class="table table-striped table-hover">
        @*<thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.First().Dept_ID)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.First().Dept_Name)
                    </th>
                    <th></th>
                </tr>
            </thead>*@
        <thead>
            <tr>
                <th>
                    Department ID
                </th>
                <th>
                    Department Name
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Dept_ID)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Dept_Name)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.Dept_ID }, new { @class = "btn btn-primary btn-sm" }) |
                        @Html.ActionLink("Details", "Details", new { id = item.Dept_ID }, new { @class = "btn btn-info btn-sm" }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.Dept_ID }, new { @class = "btn btn-danger btn-sm" })
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

