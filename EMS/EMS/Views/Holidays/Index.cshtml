@model IEnumerable<EMS.Models.Holiday>

@{
    ViewBag.Title = "Index";
}

<h2>Holidays</h2>

<p>
    @Html.ActionLink("Add New Holiday", "Create", null, new { @class = "btn btn-primary" })
</p>
@if (TempData["AlertMessage"] != null)
{
    <div id="alertMessage" class="alert alert-success">
        <strong>Sucess !</strong>@TempData["AlertMessage"]
        <button type="button" class="close" data-dismiss="alert">
            <spam>&times;</spam>
        </button>
    </div>
}

@*<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HolidayName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HolidayDate)
        </th>
        <th></th>
    </tr>*@
<div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>
                    Holiday ID
                </th>
                <th>
                    Holiday Name
                </th>
                <th>
                    Holiday Date
                </th>
            </tr>
        </thead>


        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.HolidayID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.HolidayName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.HolidayDate)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.HolidayID }, new { @class = "btn btn-primary btn-sm" }) |
                    @Html.ActionLink("Details", "Details", new { id = item.HolidayID }, new { @class = "btn btn-info btn-sm" }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.HolidayID }, new { @class = "btn btn-danger btn-sm" })
                </td>
            </tr>
        }

    </table>
</div>
